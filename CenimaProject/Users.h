#ifndef USERS_H
#define USERS_H

#include <QString>
extern QString usernames [100];
extern QString passwords [100];
extern int ages [100];
extern int usersCount; // count of the array
//creates 3 global arrays accessable in all c++ files with the heafer file included
// extern is used to declare global variables or functions in header files

#endif // USERS_H
